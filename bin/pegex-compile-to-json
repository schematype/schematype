#!/usr/bin/env perl

use strict;
use warnings;
use FindBin;
use lib "$FindBin::Bin/../lib/perl5";
use Pegex::Compiler;

my $input = do { local $/; <> };
my $compiler = Pegex::Compiler->new;

$compiler->parse($input)->combinate;

print $compiler->to_json;
