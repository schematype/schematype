- Move test suites into their code branches for now
- Move grammar branch into compiler for now

- Add enough language features to do JSC generator
  - must { 500 < .foo + .bar <= 1000 }
    { 0 < . <= 10 }  ["&&", ["<", "0", "."], ["<=", ".", "10"]]

- Write schematype-linker program
  - Use NodeJS to fetch assets?
    - Needs git, so maybe not.

- Write Core schema
  - schematype/schematype type.core.0.1.1
  - compile/link it

- Write schematype-show program

- Write schematype-generate-jsonschema

- Write schematype-validate-json
- Write schematype-validate-yaml
- Write schematype-validate-csv

- Speed up git operations with clone --depth=1 for testml

- Write documentation

== DONE
+ Use node testml runtime for compilation tests
+ Add .travis.yml for compiler testing
+ Write a .rc file in master for installation
+ Switch to new .stc data layout (note/Ideas)
+ Write `stp` CLI command in Bash
  + `stp compile` calls schematype-compiler and schematype-linker
    + `stp compile --no-link`
