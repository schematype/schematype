- Add .travis.yml for compiler testing
- Use node testml runtime for compilation tests
- Write stp CLI command in Bash
  - `stp compile` calls schematype-compiler
  - `stp link` calls schematype-linker

